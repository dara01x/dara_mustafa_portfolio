// All JSON-LD schemas are now in index.html as static <script> tags.
// Static schemas are readable by every crawler (GPTBot, PerplexityBot, ClaudeBot, etc.)
// without requiring JavaScript execution, which is the correct approach for a React SPA.
export const StructuredData = () => null;

export default StructuredData;
